#include <stdint.h>
#include <esp_err.h>


/**
 * A brief history of JavaDoc-style (C-style) comments.
 *
 * This is the typical JavaDoc-style C-style comment. It starts with two
 * asterisks.
 *
 * @param a Even if there is only one possible unified theory. it is just a
 *               set of rules and equations.
 * @param b ASD
 * 
 * @return ESP_ERR
 */
esp_err_t test_docs(uint8_t a, char* b);
